# Final UI automation project


Stepik: Selenium + Python + PageObject
https://stepik.org/course/575/syllabus

Run test:
`python -m pytest -v --tb=line --language=en -m need_review`
or
`pytest -v --tb=line --language=en -m need_review`
